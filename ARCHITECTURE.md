# 🏗️ Arquitetura Visual da FAQ API

## Fluxo Completo de Autenticação e Acesso

```
┌─────────────────────────────────────────────────────────────────────┐
│                         NUVEMSHOP OAUTH                              │
│                                                                      │
│  1. Admin clica "Instalar App"                                      │
│  2. Redirecionado para Nuvemshop para autorizar                    │
│  3. Nuvemshop gera código de autorização                           │
│  4. Callback: GET /api/ns/install?code=XXX                         │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    NuvemshopController@install                       │
│                                                                      │
│  - Recebe código                                                    │
│  - Troca por access_token                                          │
│  - Armazena em tabela stores                                       │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      BANCO DE DADOS (stores)                        │
│                                                                      │
│  store_id: "123456"                                                │
│  access_token: "token_oauth_valido"                                │
│  refresh_token: "refresh_token"                                    │
│  token_expires_at: "2025-02-09 20:00:00"                           │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Fluxo de Acesso Admin com JWT

```
┌──────────────────────────────────────────────────────────────────────┐
│                    ADMIN DASHBOARD (Frontend)                         │
│                                                                       │
│  1. Admin logado tem JWT token armazenado                            │
│  2. Faz request: POST /api/faqs                                      │
│  3. Envia header: Authorization: Bearer eyJhbGc...                  │
│                                                                       │
└──────────────────────────────────────────────────────────────────────┘
                             │
                             ▼
        ┌────────────────────────────────────┐
        │    NexoApiAuth Middleware          │
        ├────────────────────────────────────┤
        │ ① Extrai Bearer token              │
        │ ② Decodifica JWT (payload)         │
        │ ③ Lê store_id do payload           │
        │ ④ SELECT * FROM stores WHERE ...   │
        │ ⑤ ✅ Encontrou? Passa para route   │
        │ ⑥ ❌ Não achou? 404 store_not_found│
        │                                    │
        │ Anexa ao request:                  │
        │   $request->store_id = "123456"    │
        │   $request->store = Store object   │
        └────────────────────────────────────┘
                             │
                             ▼
        ┌────────────────────────────────────┐
        │      FaqController@store           │
        ├────────────────────────────────────┤
        │ 1. Extrai store_id do request      │
        │ 2. Valida dados enviados           │
        │ 3. Chama FaqService->createFaq()   │
        │ 4. Service cria registro           │
        │ 5. Retorna resposta JSON           │
        └────────────────────────────────────┘
                             │
                             ▼
        ┌────────────────────────────────────┐
        │      Banco de Dados (faqs)         │
        ├────────────────────────────────────┤
        │ INSERT INTO faqs (                 │
        │   store_id, title, active          │
        │ ) VALUES (                         │
        │   "123456", "Meu FAQ", true        │
        │ )                                  │
        └────────────────────────────────────┘
```

---

## Fluxo de Acesso Público (sem autenticação)

```
┌──────────────────────────────────────────────────────────────────────┐
│               FRONTEND CLIENTE (Página do Produto)                    │
│                                                                       │
│  GET /public/faqs/123456/product/12345                               │
│  (sem headers Authorization)                                         │
│                                                                       │
└──────────────────────────────────────────────────────────────────────┘
                             │
                             ▼
        ┌────────────────────────────────────┐
        │  FaqController@getProductFaq       │
        ├────────────────────────────────────┤
        │ 1. Recebe storeId e productId      │
        │ 2. Chama FaqService->getFaqBy...   │
        │ 3. Service procura:                │
        │    SELECT faq_bindings WHERE       │
        │      bindable_type='product' AND   │
        │      bindable_id='12345'           │
        │ 4. Retorna FAQ formatado           │
        └────────────────────────────────────┘
                             │
                             ▼
        ┌────────────────────────────────────┐
        │      Banco de Dados                │
        ├────────────────────────────────────┤
        │ SELECT faq_bindings.*,             │
        │        faqs.*,                     │
        │        faq_questions.*             │
        │ FROM faq_bindings                  │
        │ JOIN faqs ON ...                   │
        │ JOIN faq_questions ON ...          │
        │ WHERE ...                          │
        │                                    │
        │ Resultado:                         │
        │ {                                  │
        │   "id": 1,                         │
        │   "title": "Dúvidas",              │
        │   "questions": [...]               │
        │ }                                  │
        └────────────────────────────────────┘
                             │
                             ▼
        ┌────────────────────────────────────┐
        │    Response JSON para Cliente      │
        ├────────────────────────────────────┤
        │ {                                  │
        │   "id": 1,                         │
        │   "title": "...",                  │
        │   "active": true,                  │
        │   "questions": [                   │
        │     {                              │
        │       "question": "...",           │
        │       "answer": "..."              │
        │     }                              │
        │   ]                                │
        │ }                                  │
        └────────────────────────────────────┘
```

---

## Ciclo de Vida de um Binding

```
                    ┌─────────────────┐
                    │  Criar FAQ      │
                    │  ID: 1          │
                    │  Title: "FAQ"   │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │ Adicionar Q&A   │
                    │  5 perguntas    │
                    └────────┬────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
    ┌────────┐         ┌────────┐          ┌────────┐
    │Homepage│       │Product │          │Category│
    │(NULL)  │       │(12345) │          │(handle)│
    └────────┘       └────────┘          └────────┘
        │                    │                    │
        └────────────────────┼────────────────────┘
                             │
                ┌────────────┴────────────┐
                │   Binding Criado em     │
                │   faq_bindings table    │
                │                        │
                │  - faq_id: 1           │
                │  - bindable_type: ...  │
                │  - bindable_id: ...    │
                │                        │
                │  ✅ Agora está         │
                │  disponível em 3       │
                │  lugares diferentes!   │
                └────────────────────────┘
```

---

## Isolamento por Loja

```
┌─────────────────────────────────────────────────────────────────┐
│                     LOJA A (ID: 123)                            │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ FAQ 1: "Entrega"                    store_id: 123      │  │
│  │ FAQ 2: "Pagamento"                  store_id: 123      │  │
│  │ FAQ 3: "Devoluções"                 store_id: 123      │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  Admin com token JWT do storeId=123 pode ver apenas estes ↑   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                     LOJA B (ID: 456)                            │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ FAQ 1: "Frete Grátis"               store_id: 456      │  │
│  │ FAQ 2: "Rastreamento"               store_id: 456      │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  Admin com token JWT do storeId=456 pode ver apenas estes ↑   │
│  (NÃO vê os FAQs da Loja A!)                                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

                      ✅ ISOLAMENTO GARANTIDO!
```

---

## Modelo de Resposta

```
┌──────────────────────────────────────────────────────────┐
│                  RESPOSTA DE SUCESSO                     │
├──────────────────────────────────────────────────────────┤
│ HTTP 200 OK                                              │
│ Content-Type: application/json                           │
│                                                          │
│ {                                                        │
│   "success": true,                                       │
│   "data": {                                              │
│     "id": 1,                                             │
│     "title": "FAQ",                                      │
│     ...                                                  │
│   },                                                     │
│   "message": "FAQ criado com sucesso"                    │
│ }                                                        │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│                  RESPOSTA DE ERRO                        │
├──────────────────────────────────────────────────────────┤
│ HTTP 422 Unprocessable Entity                            │
│                                                          │
│ {                                                        │
│   "success": false,                                      │
│   "data": null,                                          │
│   "message": "Validation failed",                        │
│   "errors": {                                            │
│     "title": ["The title field is required"]             │
│   }                                                      │
│ }                                                        │
└──────────────────────────────────────────────────────────┘
```

---

## Hierarquia de Acesso

```
┌─────────────────────────────────────────────────────────┐
│                    CLIENTE ANÔNIMO                       │
│                                                          │
│  Pode acessar:                                          │
│  ✅ GET /public/faqs/{storeId}/product/{id}            │
│  ✅ GET /public/faqs/{storeId}/category/{handle}       │
│  ✅ GET /public/faqs/{storeId}/homepage                │
│                                                          │
│  Não pode acessar:                                      │
│  ❌ POST/PUT/DELETE /api/faqs/*                        │
│  ❌ GET /api/faqs                                       │
│                                                          │
└─────────────────────────────────────────────────────────┘
                           ▲
                           │
┌─────────────────────────────────────────────────────────┐
│             ADMIN AUTENTICADO (JWT Token)               │
│                                                          │
│  Pode acessar TUDO:                                     │
│  ✅ GET    /api/faqs                    (listar)       │
│  ✅ POST   /api/faqs                    (criar)        │
│  ✅ GET    /api/faqs/{id}               (obter)        │
│  ✅ PUT    /api/faqs/{id}               (atualizar)    │
│  ✅ DELETE /api/faqs/{id}               (deletar)      │
│  ✅ POST   /api/faqs/{id}/questions     (+ pergunta)   │
│  ✅ PUT    /api/faqs/questions/{id}     (editar)       │
│  ✅ DELETE /api/faqs/questions/{id}     (remover)      │
│  ✅ POST   /api/faqs/{id}/bindings      (vincular)     │
│  ✅ DELETE /api/faqs/bindings/{id}      (desvincular)  │
│  ✅ Tudo do acesso público                            │
│                                                          │
│  Restrição: Acessa APENAS FAQs da sua loja             │
│  (determinada pelo store_id no JWT)                    │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

## Performance & Queries

```
┌────────────────────────────────────────────────────────┐
│         OTIMIZAÇÕES IMPLEMENTADAS                      │
├────────────────────────────────────────────────────────┤
│                                                        │
│ 1️⃣  Índices em:                                       │
│    - store_id (faqs)                                  │
│    - store_id + active (faqs)                         │
│    - faq_id (faq_questions)                           │
│    - bindable_type + bindable_id (faq_bindings)       │
│                                                        │
│ 2️⃣  Eager loading com Eloquent:                       │
│    Faq::with('questions', 'bindings')->get()          │
│    (evita N+1 queries)                                │
│                                                        │
│ 3️⃣  Soft deletes POSSÍVEIS (não implementados ainda): │
│    Para manter histórico de FAQs deletados            │
│                                                        │
│ 4️⃣  Cache potencial:                                  │
│    - Cache de FAQs públicos por hora                 │
│    - Invalidar ao atualizar                           │
│                                                        │
└────────────────────────────────────────────────────────┘
```

---

## Fluxo de Desenvolvimento Recomendado

```
Day 1: Setup
├─ composer install
├─ configure .env
├─ php artisan migrate
└─ php artisan serve

Day 2: Testing Admin
├─ Criar FAQ via Postman
├─ Adicionar perguntas
├─ Vincular a produto/categoria
└─ Testar CRUD

Day 3: Testing Público
├─ Teste endpoints públicos
├─ Verificar isolamento por loja
├─ Testar retorno formatado
└─ Performance testing

Day 4: Frontend
├─ Integrar API no React
├─ Testar admin dashboard
├─ Testar exibição em páginas
└─ Deploy

Day 5+: Refinement
├─ Cache implementation
├─ Error handling
├─ Analytics
└─ Production deploy
```

---

**Status:** ✅ Arquitetura completa e pronta para desenvolvimento!
